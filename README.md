# smart_remote
all code for my smart remote

To execute the code: `./remote_venv/bin/python smart_remote.py`

All venv requirements listed under requirements.txt.
To use venv you need to source it: `source remote_venv/bin/activate.fish`. 
To update these requirements use: `./remote_venv/bin/pip freeze`.
