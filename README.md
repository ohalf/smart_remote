# smart_remote
all code for my smart remote.
To execute:
1) source remote_venv/bin/activate.fish # Loads the virtual environment with all the required libraries.
2) create a tmux session
3) python3 smart_remote.py
